; Gen 3 Shiny Odds Patcher config
; Set shiny odds as 1 in N (integer > 0)
; Modes:
; - auto/canonical/reroll: canonical PID rerolls (current-standard PKHeX-clean target)
; - native/legacy: older threshold patch behavior (non-canonical at high rates)
;
; Current public odds policy:
; - odds=64 or higher: supported target while hardening continues
; - odds=32: stretch goal, still experimental until fully validated
; - odds below 32: experimental / unsupported for legality-sensitive play
;
; Example values:
; odds=8192
; odds=256
; odds=128
; odds=64
; odds=32
; mode=auto
; mode=canonical
; mode=reroll
; mode=native
; mode=legacy
odds=64
mode=auto
